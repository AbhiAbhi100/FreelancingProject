# System Architecture Snapshots & Visual Documentation

## 📸 Snapshot 1: Complete System Overview

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         FREELANCING PLATFORM                             │
│                     Full-Stack MERN Application                          │
└─────────────────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────────────────┐
│                          FRONTEND LAYER                                 │
│  ┌──────────────────────────────────────────────────────────────────┐ │
│  │  React Application (Vite) - Port 5173                           │ │
│  │  ┌────────────┐  ┌────────────┐  ┌──────────────┐              │ │
│  │  │   Pages    │  │   Redux    │  │  Components  │              │ │
│  │  │            │  │   Store    │  │              │              │ │
│  │  │ • Home     │  │            │  │ • Navbar     │              │ │
│  │  │ • Login    │  │ • auth     │  │ • Footer     │              │ │
│  │  │ • Signup   │  │ • project  │  │ • Job Cards  │              │ │
│  │  │ • Jobs     │  │ • client   │  │ • Dialogs    │              │ │
│  │  │ • Profile  │  │ • proposal │  │ • Tables     │              │ │
│  │  │ • Admin    │  │            │  │ • Forms      │              │ │
│  │  └────────────┘  └────────────┘  └──────────────┘              │ │
│  └──────────────────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────────────────┘
                                   ▼ HTTP/HTTPS
                            (CORS Enabled)
┌────────────────────────────────────────────────────────────────────────┐
│                          BACKEND LAYER                                  │
│  ┌──────────────────────────────────────────────────────────────────┐ │
│  │  Express.js Server - Port 3000                                   │ │
│  │                                                                   │ │
│  │  ┌─────────────────────────────────────────────────────────┐    │ │
│  │  │  Middleware Layer                                        │    │ │
│  │  │  • express.json()      • cookie-parser                   │    │ │
│  │  │  • cors                • isAuthenticated                 │    │ │
│  │  │  • multer (file upload)                                  │    │ │
│  │  └─────────────────────────────────────────────────────────┘    │ │
│  │                                                                   │ │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │ │
│  │  │   Routes     │  │ Controllers  │  │   Models     │          │ │
│  │  │              │  │              │  │              │          │ │
│  │  │ user.route   │─▶│ user.ctrl    │─▶│ User         │          │ │
│  │  │ client.route │─▶│ client.ctrl  │─▶│ Client       │          │ │
│  │  │ project.route│─▶│ project.ctrl │─▶│ Project      │          │ │
│  │  │ proposal.rout│─▶│ proposal.ctrl│─▶│ Proposal     │          │ │
│  │  └──────────────┘  └──────────────┘  └──────────────┘          │ │
│  └──────────────────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────────────────┘
                                   ▼
┌────────────────────────────────────────────────────────────────────────┐
│                          DATA LAYER                                     │
│  ┌──────────────────────────────────────────────────────────────────┐ │
│  │  MongoDB Database (Mongoose ODM)                                 │ │
│  │                                                                   │ │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐        │ │
│  │  │  users   │  │ clients  │  │ projects │  │proposals │        │ │
│  │  │          │  │          │  │          │  │          │        │ │
│  │  │ Documents│  │Documents │  │Documents │  │Documents │        │ │
│  │  └──────────┘  └──────────┘  └──────────┘  └──────────┘        │ │
│  │       │             │             │             │                │ │
│  │       └─────────────┴─────────────┴─────────────┘                │ │
│  │                   Relationships via ObjectId                     │ │
│  └──────────────────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────────────────┐
│                      EXTERNAL SERVICES                                  │
│  ┌──────────────────┐              ┌────────────────────┐             │
│  │   Cloudinary     │              │   JWT Tokens       │             │
│  │  (File Storage)  │              │ (Authentication)   │             │
│  │                  │              │                    │             │
│  │ • Profile Photos │              │ • Token Generation │             │
│  │ • Portfolio Files│              │ • Token Validation │             │
│  │ • Company Logos  │              │ • Secure Cookies   │             │
│  └──────────────────┘              └────────────────────┘             │
└────────────────────────────────────────────────────────────────────────┘
```

---

## 📸 Snapshot 2: Authentication Flow

```
┌───────────────────────────────────────────────────────────────────────┐
│                     USER AUTHENTICATION FLOW                           │
└───────────────────────────────────────────────────────────────────────┘

REGISTRATION FLOW:
┌─────────┐     ┌─────────┐     ┌──────────┐     ┌──────────┐     ┌────┐
│ User    │────▶│ Upload  │────▶│ Hash     │────▶│ Create   │────▶│ DB │
│ Form    │     │ Photo   │     │ Password │     │ User     │     │    │
└─────────┘     └─────────┘     └──────────┘     └──────────┘     └────┘
    │               │                 │                │
    ├─ fullname     ├─ Cloudinary     ├─ bcrypt       ├─ Mongoose
    ├─ email        ├─ URL returned   ├─ 10 rounds    └─ users collection
    ├─ phone        └─ secure_url
    ├─ password
    ├─ role
    └─ photo file

LOGIN FLOW:
┌─────────┐     ┌─────────┐     ┌──────────┐     ┌──────────┐     ┌────┐
│ Login   │────▶│ Find    │────▶│ Verify   │────▶│ Generate │────▶│Set │
│ Form    │     │ User    │     │ Password │     │ JWT      │     │🍪 │
└─────────┘     └─────────┘     └──────────┘     └──────────┘     └────┘
    │               │                 │                │              │
    ├─ email        ├─ Query DB       ├─ bcrypt       ├─ jwt.sign    ├─ httpOnly
    ├─ password     ├─ Check role     │   compare     ├─ 1d expiry   ├─ sameSite
    └─ role         └─ User exists    └─ Match OK     └─ SECRET_KEY  └─ secure

PROTECTED ROUTE ACCESS:
┌─────────┐     ┌─────────┐     ┌──────────┐     ┌──────────┐
│ Request │────▶│ Extract │────▶│ Verify   │────▶│ Attach   │────▶ Continue
│ w/ 🍪   │     │ Token   │     │ JWT      │     │ userId   │
└─────────┘     └─────────┘     └──────────┘     └──────────┘
                                     │
                                     └─ Invalid ──▶ 401 Unauthorized
```

---

## 📸 Snapshot 3: Data Model Relationships

```
┌────────────────────────────────────────────────────────────────────────┐
│                     DATABASE ENTITY RELATIONSHIPS                       │
└────────────────────────────────────────────────────────────────────────┘

                    ┌─────────────────────┐
                    │       USER          │
                    │ ─────────────────── │
                    │ _id (PK)            │
                    │ fullname            │
                    │ email (UNIQUE)      │
                    │ phoneNumber         │
                    │ password (hashed)   │
                    │ role: enum          │
                    │ profile:            │
                    │   • bio             │
                    │   • skills[]        │
                    │   • portfolio       │
                    │   • hourlyRate      │
                    │   • profilePhoto    │
                    │ timestamps          │
                    └─────────────────────┘
                          │       │
                 creates  │       │ submits
                 (1:n)    │       │ (1:n)
                          ▼       ▼
        ┌─────────────────────┐   ┌─────────────────────┐
        │      CLIENT         │   │     PROPOSAL        │
        │ ─────────────────── │   │ ─────────────────── │
        │ _id (PK)            │   │ _id (PK)            │
        │ name (UNIQUE)       │   │ project (FK) ───────┼───┐
        │ description         │   │ freelancer (FK) ────┼───┤
        │ website             │   │ status: enum        │   │
        │ location            │   │   • pending         │   │
        │ logo                │   │   • accepted        │   │
        │ userId (FK) ────────┼─┐ │   • rejected        │   │
        │ timestamps          │ │ │ timestamps          │   │
        └─────────────────────┘ │ └─────────────────────┘   │
                │               │           ▲               │
        owns    │ (1:n)         │           │ receives      │
                │               │           │ (1:n)         │
                ▼               │           │               │
        ┌─────────────────────┐ │   ┌───────┴─────────────┐ │
        │      PROJECT        │ │   │      PROJECT        │◀┘
        │ ─────────────────── │ │   │ ─────────────────── │
        │ _id (PK)            │ │   │ _id (PK)            │
        │ title               │ │   │ proposals[] (FK)    │
        │ description         │ │   └─────────────────────┘
        │ requirements[]      │ │
        │ budget              │ │
        │ experienceLevel     │ │
        │ location            │ │
        │ projectType         │ │
        │ duration            │ │
        │ client (FK) ────────┼─┘
        │ created_by (FK) ────┼───▶ (references USER._id)
        │ proposals[] (FK)    │
        │ timestamps          │
        └─────────────────────┘

RELATIONSHIP SUMMARY:
═══════════════════════════════════════════════════════════════════════
Entity        │ Relationship │ Entity     │ Type │ Description
──────────────┼──────────────┼────────────┼──────┼──────────────────
User          │ creates      │ Client     │ 1:N  │ User can create multiple clients
User          │ creates      │ Project    │ 1:N  │ User can post multiple projects
User          │ submits      │ Proposal   │ 1:N  │ Freelancer submits multiple proposals
Client        │ owns         │ Project    │ 1:N  │ Client has multiple projects
Project       │ receives     │ Proposal   │ 1:N  │ Project receives multiple proposals
═══════════════════════════════════════════════════════════════════════
```

---

## 📸 Snapshot 4: API Request/Response Flow

```
┌────────────────────────────────────────────────────────────────────────┐
│                    TYPICAL API REQUEST FLOW                             │
└────────────────────────────────────────────────────────────────────────┘

Example: Freelancer Applies to Project

1. CLIENT SIDE (React/Redux)
   ┌──────────────────────────────────────────┐
   │ User clicks "Apply" button               │
   │ ↓                                        │
   │ Dispatch Redux action                    │
   │ ↓                                        │
   │ axios.get(`/api/v1/proposal/apply/${id}`)│
   │ with credentials & auth token            │
   └──────────────────────────────────────────┘
                    │
                    │ HTTP GET Request
                    │ Cookie: token=jwt_token_here
                    │
                    ▼
2. BACKEND - MIDDLEWARE
   ┌──────────────────────────────────────────┐
   │ Express receives request                 │
   │ ↓                                        │
   │ CORS middleware validates origin         │
   │ ↓                                        │
   │ cookieParser extracts token              │
   │ ↓                                        │
   │ isAuthenticated middleware               │
   │   • Verifies JWT token                   │
   │   • Decodes userId                       │
   │   • Attaches req.id = userId             │
   └──────────────────────────────────────────┘
                    │
                    ▼
3. BACKEND - ROUTING
   ┌──────────────────────────────────────────┐
   │ Route: /api/v1/proposal/apply/:id        │
   │ ↓                                        │
   │ Matches route pattern                    │
   │ ↓                                        │
   │ Calls: proposalController.applyProposal  │
   └──────────────────────────────────────────┘
                    │
                    ▼
4. BACKEND - CONTROLLER
   ┌──────────────────────────────────────────┐
   │ applyProposal function executes:         │
   │                                          │
   │ Step 1: Extract projectId from params   │
   │         const projectId = req.params.id  │
   │                                          │
   │ Step 2: Get userId from middleware       │
   │         const userId = req.id            │
   │                                          │
   │ Step 3: Validate project exists          │
   │         Project.findById(projectId)      │
   │         ↓                                │
   │         if (!project) return 404         │
   │                                          │
   │ Step 4: Check for duplicate              │
   │         Proposal.findOne({               │
   │           project: projectId,            │
   │           freelancer: userId             │
   │         })                               │
   │         ↓                                │
   │         if (exists) return 400           │
   │                                          │
   │ Step 5: Create new proposal              │
   │         Proposal.create({                │
   │           project: projectId,            │
   │           freelancer: userId,            │
   │           status: 'pending'              │
   │         })                               │
   │                                          │
   │ Step 6: Update project's proposals array │
   │         project.proposals.push(proposal) │
   │         project.save()                   │
   │                                          │
   │ Step 7: Send success response            │
   │         res.status(201).json({...})      │
   └──────────────────────────────────────────┘
                    │
                    ▼
5. DATABASE OPERATIONS
   ┌──────────────────────────────────────────┐
   │ MongoDB Operations:                      │
   │                                          │
   │ [Query 1] Find project                   │
   │   db.projects.findOne({_id: projectId})  │
   │   ↓                                      │
   │   Returns project document               │
   │                                          │
   │ [Query 2] Check duplicate proposal       │
   │   db.proposals.findOne({                 │
   │     project: projectId,                  │
   │     freelancer: userId                   │
   │   })                                     │
   │   ↓                                      │
   │   Returns null (no duplicate)            │
   │                                          │
   │ [Insert] Create new proposal             │
   │   db.proposals.insertOne({               │
   │     _id: new ObjectId(),                 │
   │     project: projectId,                  │
   │     freelancer: userId,                  │
   │     status: 'pending',                   │
   │     createdAt: new Date(),               │
   │     updatedAt: new Date()                │
   │   })                                     │
   │   ↓                                      │
   │   Returns inserted document              │
   │                                          │
   │ [Update] Add proposal to project         │
   │   db.projects.updateOne(                 │
   │     {_id: projectId},                    │
   │     {$push: {proposals: proposalId}}     │
   │   )                                      │
   └──────────────────────────────────────────┘
                    │
                    ▼
6. RESPONSE TO CLIENT
   ┌──────────────────────────────────────────┐
   │ HTTP 201 Created                         │
   │ Content-Type: application/json           │
   │                                          │
   │ {                                        │
   │   "message": "Applied successfully",     │
   │   "success": true                        │
   │ }                                        │
   └──────────────────────────────────────────┘
                    │
                    ▼
7. CLIENT SIDE - RESPONSE HANDLING
   ┌──────────────────────────────────────────┐
   │ Redux receives response                  │
   │ ↓                                        │
   │ Update state with new proposal           │
   │ ↓                                        │
   │ Show success toast notification          │
   │ ↓                                        │
   │ Re-render UI components                  │
   └──────────────────────────────────────────┘
```

---

## 📸 Snapshot 5: File Upload Process

```
┌────────────────────────────────────────────────────────────────────────┐
│                     FILE UPLOAD WORKFLOW                                │
└────────────────────────────────────────────────────────────────────────┘

SCENARIO: User uploads profile photo during registration

┌─────────────┐
│   FRONTEND  │
│   (React)   │
└──────┬──────┘
       │
       │ 1. User selects image file
       │    <input type="file" />
       │
       ▼
┌──────────────────────────────┐
│ FormData Object Creation     │
│                              │
│ const formData = new FormData()
│ formData.append('fullname', fullname)
│ formData.append('email', email)
│ formData.append('file', selectedFile)
│ formData.append('password', password)
│ formData.append('role', role)
└──────────────────────────────┘
       │
       │ 2. POST request to /api/v1/user/register
       │    Content-Type: multipart/form-data
       │
       ▼
┌──────────────────────────────┐
│   BACKEND - MIDDLEWARE       │
│   (Multer)                   │
│                              │
│ const storage = multer       │
│   .memoryStorage()           │
│                              │
│ const upload = multer({      │
│   storage                    │
│ })                           │
│                              │
│ .single('file')              │
└──────────────────────────────┘
       │
       │ 3. File stored in memory buffer
       │    req.file = {
       │      fieldname: 'file',
       │      originalname: 'photo.jpg',
       │      mimetype: 'image/jpeg',
       │      buffer: <Buffer...>,
       │      size: 52394
       │    }
       │
       ▼
┌──────────────────────────────┐
│   BACKEND - CONTROLLER       │
│   (register function)        │
│                              │
│ const file = req.file        │
└──────────────────────────────┘
       │
       │ 4. Convert to Data URI
       │
       ▼
┌──────────────────────────────┐
│   UTILS - DataURI            │
│                              │
│ const fileUri = getDataUri(file)
│                              │
│ Returns:                     │
│ {                            │
│   content: "data:image/jpeg;base64,/9j/4AAQ..."
│ }                            │
└──────────────────────────────┘
       │
       │ 5. Upload to cloud
       │
       ▼
┌──────────────────────────────┐
│   CLOUDINARY SERVICE         │
│                              │
│ cloudinary.uploader.upload(  │
│   fileUri.content            │
│ )                            │
│                              │
│ Processing:                  │
│ • Optimize image             │
│ • Generate thumbnail         │
│ • Store in cloud             │
│ • Return secure URL          │
└──────────────────────────────┘
       │
       │ 6. Response from Cloudinary
       │    {
       │      secure_url: "https://res.cloudinary.com/...",
       │      public_id: "user_photos/abc123",
       │      format: "jpg",
       │      width: 1200,
       │      height: 900
       │    }
       │
       ▼
┌──────────────────────────────┐
│   BACKEND - CONTROLLER       │
│   (Save to database)         │
│                              │
│ User.create({                │
│   fullname,                  │
│   email,                     │
│   password: hashedPassword,  │
│   profile: {                 │
│     profilePhoto: cloudResponse.secure_url
│   }                          │
│ })                           │
└──────────────────────────────┘
       │
       │ 7. Success response
       │
       ▼
┌──────────────────────────────┐
│   FRONTEND                   │
│   (Display success)          │
│                              │
│ • Show success message       │
│ • Display uploaded image     │
│ • Redirect to login          │
└──────────────────────────────┘

FILE UPLOAD ENDPOINTS:
═══════════════════════════════════════════════════════════════
Endpoint                    │ File Type        │ Purpose
────────────────────────────┼──────────────────┼──────────────
/api/v1/user/register       │ Profile Photo    │ User registration
/api/v1/user/profile/update │ Profile/Portfolio│ Profile update
/api/v1/client/register     │ Company Logo     │ Client registration
/api/v1/client/update/:id   │ Company Logo     │ Client update
═══════════════════════════════════════════════════════════════
```

---

## 📸 Snapshot 6: Redux State Management

```
┌────────────────────────────────────────────────────────────────────────┐
│                    REDUX STORE STRUCTURE                                │
└────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│                         ROOT STORE                                   │
│                                                                      │
│  ┌────────────────────┐  ┌────────────────────┐                   │
│  │   authSlice        │  │   projectSlice     │                   │
│  │  ────────────────  │  │  ────────────────  │                   │
│  │  • user            │  │  • allProjects     │                   │
│  │  • loading         │  │  • singleProject   │                   │
│  │  • token           │  │  • adminProjects   │                   │
│  │                    │  │  • searchQuery     │                   │
│  │  Actions:          │  │  • searchedProjects│                   │
│  │  - setUser()       │  │  • loading         │                   │
│  │  - setLoading()    │  │                    │                   │
│  │  - setToken()      │  │  Actions:          │                   │
│  │                    │  │  - setAllProjects()│                   │
│  └────────────────────┘  │  - setSingleProject()                  │
│                          │  - setSearchQuery()│                   │
│                          └────────────────────┘                   │
│                                                                     │
│  ┌────────────────────┐  ┌────────────────────┐                   │
│  │   clientSlice      │  │  proposalSlice     │                   │
│  │  ────────────────  │  │  ────────────────  │                   │
│  │  • singleClient    │  │  • allAppliedProposals                │
│  │  • allClients      │  │  • allApplicants   │                   │
│  │  • searchClient    │  │                    │                   │
│  │                    │  │  Actions:          │                   │
│  │  Actions:          │  │  - setAllAppliedProposals()           │
│  │  - setSingleClient()  │  - setAllApplicants()                 │
│  │  - setAllClients() │  │                    │                   │
│  │  - setSearchClient()  │                    │                   │
│  └────────────────────┘  └────────────────────┘                   │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘

STATE PERSISTENCE:
───────────────────────────────────────────────────────────────────────
Using redux-persist with localStorage

Persisted Data:
• User authentication state
• User profile information
• JWT token
• Last viewed projects
• Search queries
• Client information

Benefits:
✓ Survives page refreshes
✓ Faster page loads
✓ Improved UX
✓ Offline capability


TYPICAL STATE FLOW:
───────────────────────────────────────────────────────────────────────

1. LOGIN FLOW:
   User fills form → dispatch login action → API call
   → Success → dispatch setUser(userData) → dispatch setToken(token)
   → Update UI

2. PROJECT BROWSING:
   Page load → dispatch getAllProjects() → API call
   → Success → dispatch setAllProjects(projects)
   → Components re-render with data

3. SEARCH FUNCTIONALITY:
   User types → dispatch setSearchQuery(query)
   → Filter projects → dispatch setSearchedProjects(filtered)
   → Display results

4. PROPOSAL SUBMISSION:
   Click apply → dispatch applyProposal() → API call
   → Success → Update local state → Show notification
   → Refresh proposals list
```

---

## 📸 Snapshot 7: Security Architecture

```
┌────────────────────────────────────────────────────────────────────────┐
│                      SECURITY IMPLEMENTATION                            │
└────────────────────────────────────────────────────────────────────────┘

1. PASSWORD SECURITY
   ═════════════════════════════════════════════════════════════════

   Registration:
   ┌──────────────┐     ┌──────────────┐     ┌──────────────┐
   │ Plain        │────▶│ bcrypt.hash()│────▶│ Hashed       │
   │ Password     │     │ (10 rounds)  │     │ Password     │
   │ "MyPass123"  │     │              │     │ "$2a$10$..." │
   └──────────────┘     └──────────────┘     └──────────────┘
                                                     │
                                                     ▼
                                              [Stored in DB]

   Login:
   ┌──────────────┐     ┌──────────────┐     ┌──────────────┐
   │ User Input   │────▶│bcrypt.compare│────▶│ Match?       │
   │ "MyPass123"  │     │ with DB hash │     │ ✓ / ✗        │
   └──────────────┘     └──────────────┘     └──────────────┘

2. JWT AUTHENTICATION
   ═════════════════════════════════════════════════════════════════

   Token Structure:
   ┌─────────────────────────────────────────────────────────────┐
   │ HEADER.PAYLOAD.SIGNATURE                                    │
   │                                                             │
   │ Header:                                                     │
   │ { "alg": "HS256", "typ": "JWT" }                           │
   │                                                             │
   │ Payload:                                                    │
   │ {                                                           │
   │   "userId": "64abc123...",                                  │
   │   "iat": 1699632000,                                        │
   │   "exp": 1699718400                                         │
   │ }                                                           │
   │                                                             │
   │ Signature:                                                  │
   │ HMACSHA256(                                                 │
   │   base64UrlEncode(header) + "." +                           │
   │   base64UrlEncode(payload),                                 │
   │   SECRET_KEY                                                │
   │ )                                                           │
   └─────────────────────────────────────────────────────────────┘

   Token Lifecycle:
   Generate → Store in Cookie → Send with Requests → Verify → Refresh/Expire

3. COOKIE SECURITY
   ═════════════════════════════════════════════════════════════════

   Cookie Configuration:
   ┌────────────────────────────────────────────────────────────┐
   │ res.cookie("token", jwt_token, {                           │
   │   maxAge: 86400000,        // 24 hours                     │
   │   httpOnly: true,          // ✓ No JavaScript access       │
   │   sameSite: 'strict',      // ✓ CSRF protection            │
   │   secure: true             // ✓ HTTPS only (production)    │
   │ })                                                         │
   └────────────────────────────────────────────────────────────┘

4. CORS PROTECTION
   ═════════════════════════════════════════════════════════════════

   Configuration:
   ┌────────────────────────────────────────────────────────────┐
   │ const corsOptions = {                                      │
   │   origin: 'http://localhost:5173',  // Only frontend      │
   │   credentials: true                  // Allow cookies      │
   │ }                                                          │
   │                                                            │
   │ app.use(cors(corsOptions))                                 │
   └────────────────────────────────────────────────────────────┘

   Request Flow:
   Frontend (5173) ──[Allow]──▶ Backend (3000)
   Other Origins   ──[Block]──X Backend (3000)

5. MIDDLEWARE AUTHENTICATION
   ═════════════════════════════════════════════════════════════════

   isAuthenticated Middleware:
   ┌────────────────────────────────────────────────────────────┐
   │ export const isAuthenticated = async (req, res, next) => { │
   │                                                            │
   │   1. Extract token from cookies                           │
   │      const token = req.cookies.token                       │
   │                                                            │
   │   2. Check if token exists                                │
   │      if (!token) return 401 Unauthorized                   │
   │                                                            │
   │   3. Verify token with SECRET_KEY                         │
   │      const decoded = jwt.verify(token, SECRET_KEY)         │
   │                                                            │
   │   4. Attach userId to request                             │
   │      req.id = decoded.userId                               │
   │                                                            │
   │   5. Continue to next middleware/controller               │
   │      next()                                                │
   │ }                                                          │
   └────────────────────────────────────────────────────────────┘

6. ROLE-BASED ACCESS CONTROL
   ═════════════════════════════════════════════════════════════════

   Protected Routes:
   ┌────────────────────────────────────────────────────────────┐
   │ Route                     │ Role Required                  │
   │ ─────────────────────────┼────────────────────────────    │
   │ POST /project/post        │ Client only                    │
   │ GET /project/getclient... │ Client only                    │
   │ GET /proposal/apply/:id   │ Freelancer only                │
   │ GET /proposal/get         │ Freelancer only                │
   │ GET /:id/applicants       │ Client only                    │
   │ POST /status/:id/update   │ Client only                    │
   └────────────────────────────────────────────────────────────┘

7. INPUT VALIDATION
   ═════════════════════════════════════════════════════════════════

   Server-side Validation:
   ✓ Required field checks
   ✓ Email format validation
   ✓ Password strength (enforced)
   ✓ Role enum validation
   ✓ Unique constraint checks
   ✓ File type validation
   ✓ File size limits

8. SECURE FILE UPLOAD
   ═════════════════════════════════════════════════════════════════

   Protection Measures:
   ✓ Memory storage (no disk writes)
   ✓ File type restriction
   ✓ Size limits via Multer
   ✓ Direct upload to Cloudinary
   ✓ Secure URLs returned
   ✓ No public file access on server
```

---

## 📸 Snapshot 8: Project Structure Tree

```
FreelancingProject/
│
├── 📁 backend/
│   ├── 📄 .env                         ← Environment variables
│   ├── 📄 .gitignore                   ← Git ignore rules
│   ├── 📄 index.js                     ← Server entry point
│   ├── 📄 package.json                 ← Dependencies & scripts
│   │
│   ├── 📁 controllers/                 ← Business logic layer
│   │   ├── 📄 client.controller.js     • registerClient()
│   │   ├── 📄 project.controller.js    • postProject()
│   │   ├── 📄 proposal.controller.js   • applyProposal()
│   │   └── 📄 user.controller.js       • register(), login()
│   │
│   ├── 📁 middlewares/                 ← Request interceptors
│   │   ├── 📄 isAuthenticated.js       • JWT verification
│   │   └── 📄 mutler.js                • File upload handling
│   │
│   ├── 📁 models/                      ← Data schemas
│   │   ├── 📄 client.model.js          • Client schema
│   │   ├── 📄 project.model.js         • Project schema
│   │   ├── 📄 proposal.model.js        • Proposal schema
│   │   └── 📄 user.model.js            • User schema
│   │
│   ├── 📁 routes/                      ← API endpoints
│   │   ├── 📄 client.route.js          • /api/v1/client/*
│   │   ├── 📄 project.route.js         • /api/v1/project/*
│   │   ├── 📄 proposal.route.js        • /api/v1/proposal/*
│   │   └── 📄 user.route.js            • /api/v1/user/*
│   │
│   └── 📁 utils/                       ← Helper functions
│       ├── 📄 cloudinary.js            • Cloud storage config
│       ├── 📄 datauri.js               • File conversion
│       └── 📄 db.js                    • MongoDB connection
│
├── 📁 frontend/
│   ├── 📄 .eslintrc.cjs                ← ESLint config
│   ├── 📄 .gitignore                   ← Git ignore rules
│   ├── 📄 components.json              ← UI component config
│   ├── 📄 index.html                   ← HTML entry
│   ├── 📄 jsconfig.json                ← JS path aliases
│   ├── 📄 package.json                 ← Dependencies & scripts
│   ├── 📄 postcss.config.js            ← PostCSS config
│   ├── 📄 README.md                    ← Frontend docs
│   ├── 📄 tailwind.config.js           ← Tailwind CSS config
│   ├── 📄 vercel.json                  ← Deployment config
│   ├── 📄 vite.config.js               ← Vite bundler config
│   │
│   ├── 📁 public/                      ← Static assets
│   │
│   └── 📁 src/
│       ├── 📄 App.css                  ← Global styles
│       ├── 📄 App.jsx                  ← Root component & routes
│       ├── 📄 index.css                ← Base styles
│       ├── 📄 main.jsx                 ← React entry point
│       │
│       ├── 📁 assets/                  ← Images, icons, etc.
│       │
│       ├── 📁 components/              ← React components
│       │   ├── 📄 Home.jsx             • Landing page
│       │   ├── 📄 Jobs.jsx             • Project listing
│       │   ├── 📄 JobDescription.jsx   • Project details
│       │   ├── 📄 Profile.jsx          • User profile
│       │   ├── 📄 Browse.jsx           • Browse projects
│       │   │
│       │   ├── 📁 admin/               ← Client dashboard
│       │   │   ├── 📄 Companies.jsx    • Client management
│       │   │   ├── 📄 CompanyCreate.jsx• Create client
│       │   │   ├── 📄 CompanySetup.jsx • Edit client
│       │   │   ├── 📄 AdminJobs.jsx    • Posted projects
│       │   │   ├── 📄 PostJob.jsx      • Create project
│       │   │   ├── 📄 Applicants.jsx   • View proposals
│       │   │   └── 📄 ProtectedRoute.jsx• Route guard
│       │   │
│       │   ├── 📁 auth/                ← Authentication
│       │   │   ├── 📄 Login.jsx        • Login form
│       │   │   └── 📄 Signup.jsx       • Registration form
│       │   │
│       │   ├── 📁 shared/              ← Reusable components
│       │   │   ├── 📄 Navbar.jsx       • Navigation bar
│       │   │   └── 📄 Footer.jsx       • Footer
│       │   │
│       │   └── 📁 ui/                  ← UI primitives
│       │       ├── 📄 avatar.jsx       • Avatar component
│       │       ├── 📄 badge.jsx        • Badge component
│       │       ├── 📄 button.jsx       • Button component
│       │       ├── 📄 dialog.jsx       • Modal component
│       │       ├── 📄 input.jsx        • Input component
│       │       ├── 📄 table.jsx        • Table component
│       │       └── ... (more UI components)
│       │
│       ├── 📁 hooks/                   ← Custom React hooks
│       │   ├── 📄 useGetAllJobs.jsx    • Fetch all projects
│       │   ├── 📄 useGetAllAdminJobs.jsx• Fetch client projects
│       │   ├── 📄 useGetAllCompanies.jsx• Fetch clients
│       │   ├── 📄 useGetCompanyById.jsx• Fetch single client
│       │   └── 📄 useGetAppliedJobs.jsx• Fetch proposals
│       │
│       ├── 📁 lib/                     ← Utilities
│       │   └── 📄 utils.js             • Helper functions
│       │
│       ├── 📁 redux/                   ← State management
│       │   ├── 📄 store.js             • Redux store config
│       │   ├── 📄 authSlice.js         • Auth state
│       │   ├── 📄 clientSlice.js       • Client state
│       │   ├── 📄 projectSlice.js      • Project state
│       │   └── 📄 proposalSlice.js     • Proposal state
│       │
│       └── 📁 utils/                   ← Frontend utilities
│           └── 📄 constant.js          • API constants
│
└── 📄 README.md                        ← Project documentation

KEY FILES EXPLAINED:
══════════════════════════════════════════════════════════════════════
File                    │ Purpose
────────────────────────┼──────────────────────────────────────────────
backend/index.js        │ Express server setup, middleware, routes
backend/.env            │ DATABASE_URL, SECRET_KEY, CLOUDINARY_*
frontend/src/App.jsx    │ React Router configuration
frontend/src/main.jsx   │ React app initialization with Redux
frontend/vite.config.js │ Dev server, proxy, build configuration
package.json (both)     │ Dependencies, scripts, metadata
```

---

_Documentation Generated: November 10, 2025_  
_FreelancingProject v1.0_  
_Full-Stack MERN Application_
